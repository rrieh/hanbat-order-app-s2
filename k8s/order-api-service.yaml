apiVersion: v1
kind: Service
metadata:
  name: order-api
  namespace: hanbat-<본인_GitHub_사용자명>
  labels:
    app: order-api
spec:
  selector:
    app: order-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
