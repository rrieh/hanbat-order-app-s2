apiVersion: v1
kind: Service
metadata:
  name: payment-api
  namespace: hanbat-<본인_GitHub_사용자명>
  labels:
    app: payment-api
spec:
  selector:
    app: payment-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
